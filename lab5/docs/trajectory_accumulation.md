# Накопление истории трекинга

## 📝 Новое поведение

### Результаты остаются на экране
- При переключении на другой кадр **результаты обработки НЕ сбрасываются**
- Визуализация остается видимой до следующей обработки
- Статус показывает: "⚠ Показаны результаты кадра X, текущий кадр: Y"

### Траектории накапливаются
- Трекер **НЕ сбрасывается** между обработками разных кадров
- Траектории объектов **накапливаются** от кадра к кадру
- Можно отследить полный путь объекта через все обработанные кадры

---

## 🎯 Как это работает

### Сценарий: Отслеживание машины через несколько кадров

```
Кадр 10: Обработать
  ↓
Машина ID 1 появляется в позиции (100, 100)
Траектория: [(100, 100)]
  ↓
Кадр 20: Переключение → Результаты кадра 10 остаются на экране
Статус: "⚠ Показаны результаты кадра 10, текущий кадр: 20"
  ↓
Кадр 20: Обработать
  ↓
Машина ID 1 в позиции (200, 100)
Траектория: [(100, 100), (110, 100), (120, 100), ..., (200, 100)]
  ↓
Кадр 30: Переключение → Результаты кадра 20 остаются
  ↓
Кадр 30: Обработать
  ↓
Машина ID 1 в позиции (300, 100)
Траектория: [(100, 100), ..., (200, 100), ..., (300, 100)]
```

**Результат:** Вы видите полный путь машины от кадра 10 до кадра 30!

---

## 🔄 Сброс истории

### Когда нужно сбросить?
- Начать отслеживание новой сцены
- Очистить накопленные траектории
- Начать заново с другими параметрами

### Как сбросить?
Нажмите кнопку **"🔄 Сбросить историю трекинга"**

**Что произойдет:**
- Все траектории очищаются
- ID объектов сбрасываются (начнутся с 0)
- Результаты визуализации очищаются
- Статус: "🔄 История трекинга сброшена"

---

## 💡 Примеры использования

### Пример 1: Отслеживание одной машины

```
1. Кадр 50: Обработать
   → Машина ID 3 появляется

2. Кадр 60: Обработать
   → Машина ID 3 продолжает движение
   → Траектория показывает путь от кадра 50 до 60

3. Кадр 70: Обработать
   → Машина ID 3 продолжает
   → Траектория от кадра 50 до 70

4. Кадр 80: Обработать
   → Машина ID 3 уехала из кадра
   → Траектория полностью видна
```

### Пример 2: Анализ перекрестка

```
1. Кадр 100: Обработать
   → 3 машины: ID 1, 2, 3

2. Кадр 110: Обработать
   → Машина ID 1 уехала
   → Машины ID 2, 3 продолжают
   → Новая машина ID 4 появилась

3. Кадр 120: Обработать
   → Машины ID 2, 3, 4 продолжают
   → Траектории показывают их пути

4. Нажать "🔄 Сбросить историю трекинга"
   → Начать отслеживание новой волны машин
```

### Пример 3: Сравнение разных участков видео

```
1. Кадры 10-30: Обработать несколько кадров
   → Накопить траектории для первого участка

2. Нажать "🔄 Сбросить историю трекинга"

3. Кадры 200-220: Обработать несколько кадров
   → Накопить траектории для второго участка

4. Сравнить плотность трафика на разных участках
```

---

## 🎨 Визуальные индикаторы

### Статусы

**Зеленый (жирный):**
```
✓ Кадр 50: 3 объект(ов), 2.34с
```
Только что обработан кадр 50, найдено 3 объекта.

**Оранжевый (курсив):**
```
⚠ Показаны результаты кадра 50, текущий кадр: 60
```
На экране результаты кадра 50, но вы переключились на кадр 60.

**Синий (курсив):**
```
🔄 История трекинга сброшена
```
Трекер сброшен, готов к новой истории.

---

## ⚙️ Технические детали

### Когда трекер сбрасывается?

1. **При загрузке нового видео** (автоматически)
2. **При нажатии "🔄 Сбросить историю трекинга"** (вручную)

### Когда трекер НЕ сбрасывается?

1. При переключении между кадрами
2. При изменении параметров алгоритма
3. При изменении режима визуализации
4. При обработке следующего кадра

### Как работает накопление траекторий?

```python
# CentroidTracker хранит траектории в deque
self.paths[object_id] = deque(maxlen=30)

# При каждом обновлении добавляется новая точка
self.paths[object_id].append((cx, cy))

# Траектория автоматически ограничена 30 последними точками
# Но ID объекта сохраняется между кадрами!
```

**Важно:** Траектория показывает последние 30 точек, но ID объекта сохраняется бесконечно (пока объект не исчезнет на `max_disappeared` кадров).

---

## 🔍 Отличия от старого поведения

### Было (старое):
```
Переключение кадра → Результаты очищаются
                   → Трекер сбрасывается
                   → Траектории теряются
```

### Стало (новое):
```
Переключение кадра → Результаты остаются на экране
                   → Трекер сохраняет состояние
                   → Траектории накапливаются
                   → Статус предупреждает о несоответствии
```

---

## 🎓 Преимущества

1. **Видна полная история движения** объекта
2. **Можно отследить объект через весь видеоряд**
3. **Не нужно обрабатывать каждый кадр** - достаточно ключевых
4. **Траектории наглядно показывают паттерны движения**

---

## ⚠️ Важные замечания

### Ограничение траектории (30 точек)
- Траектория показывает последние 30 точек центроида
- Это сделано для производительности и читаемости
- Если нужна полная история, можно увеличить в `core/object_tracker.py`:
  ```python
  self.paths[object_id] = deque(maxlen=100)  # Вместо 30
  ```

### Параметр max_disappeared
- Если объект исчезает на > `max_disappeared` кадров, его ID удаляется
- При повторном появлении получит новый ID
- Для длинных исчезновений увеличьте параметр

### Сброс при загрузке видео
- При загрузке нового видео трекер автоматически сбрасывается
- Это предотвращает смешивание объектов из разных видео

---

**Теперь вы можете отслеживать полную историю движения объектов!** 🎉
